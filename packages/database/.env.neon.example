# Neon Database Configuration Example
# Copy this to packages/database/prisma/.env and fill in your values

# For Prisma 5.10+ (including 6.x), you only need ONE connection string!
# Prisma automatically handles pooled vs direct connections with Neon

# Get this from your Neon dashboard:
# 1. Go to your project dashboard
# 2. Click on "Connection Details"
# 3. Toggle "Pooled connection" ON
# 4. Copy the connection string

# Pooled connection (Prisma will use this for queries AND handle migrations automatically)
DATABASE_URL="postgresql://username:password@project-name-pooler.region.neon.tech/database-name?sslmode=require"

# Example with actual format:
# DATABASE_URL="postgresql://alex:AbC123@my-project-pooler.us-east-2.aws.neon.tech/neondb?sslmode=require"

# That's it! No DIRECT_URL needed anymore 🎉